apiVersion: v1
kind: Service
metadata:
  name: net-watch-api-svc
  namespace: default
  labels:
    app: net-watch-api
spec:
  selector:
    app: net-watch-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080